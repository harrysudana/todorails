<h1>Show Task</h1>
<p>
  <strong>Title:</strong>
  <%= @task.title %>
</p>
 
<p>
  <strong>Note:</strong>
  <%= @task.note %>
</p>
 
<h2>Comments</h2>
<%= render @task.comments %>
 
<h2>Add a comment:</h2>
<%= render 'comments/form' %>

<%= link_to 'Edit', edit_task_path(@task) %> |
<%= link_to 'Back', tasks_path %>